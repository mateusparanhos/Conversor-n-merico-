import conversao as cvs

texto = input('Digite um número por extenso ou com k, m, g no lugar de mil, milhão e bilhão para tradução para ver o número em algoritsmos:\n')

print(cvs.converte_para_numero(texto))
